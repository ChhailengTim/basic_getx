import 'package:flutter/material.dart';

class MainSldie extends StatelessWidget {
  const MainSldie({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
