import 'package:flutter/material.dart';

class WebViewHTML extends StatelessWidget {
  const WebViewHTML({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('WebViewHTML'),
      ),
    );
  }
}
